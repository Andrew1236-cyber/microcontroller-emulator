# Эмулятор Микроконтроллера

## Описание
Простой эмулятор 8-битного микроконтроллера с собственной системой команд, написанный на Python.

## Архитектура
- 8-битная архитектура
- 16 регистров общего назначения (R0-R15)
- 64KB адресного пространства
- 16 инструкций

## Система команд

| Инструкция | Описание | Код |
|------------|----------|-----|
| NOP        | Нет операции | 0x00 |
| LDI Rd, imm | Загрузка непосредственного значения | 0x01 |
| MOV Rd, Rs | Копирование регистров | 0x02 |
| ADD Rd, Rs | Сложение | 0x03 |
| SUB Rd, Rs | Вычитание | 0x04 |
| JMP addr   | Безусловный переход | 0x05 |
| JZ addr    | Переход если Z=1 | 0x06 |
| JNZ addr   | Переход если Z=0 | 0x07 |
| CALL addr  | Вызов подпрограммы | 0x08 |
| RET        | Возврат из подпрограммы | 0x09 |
| PUSH Rs    | Поместить в стек | 0x0A |
| POP Rd     | Извлечь из стека | 0x0B |
| AND Rd, Rs | Побитовое И | 0x0C |
| OR Rd, Rs  | Побитовое ИЛИ | 0x0D |
| XOR Rd, Rs | Исключающее ИЛИ | 0x0E |
| HALT       | Остановка | 0x0F |

## Использование

1. Запустите эмулятор:
```bash
python emulator.py




СРАВНЕНИЕ С РАБОТОЙ --- https://github.com/Xwesxae/STM32-.git
Тип проекта:

Первая работа: эмулятор микроконтроллера (виртуальная машина) с собственной системой команд, написанный на Python.

Вторая работа: прошивка (реальный код) для конкретного микроконтроллера (STM32F103C8T6), которая запускается в эмуляторе Renode.

Уровень абстракции:

Первая: эмулятор, который имитирует работу микроконтроллера, включая процессор, память, инструкции.

Вторая: программа, которая выполняется на реальном (или эмулируемом) железе, используя конкретную периферию (UART).

Язык реализации:

Первая: Python (эмулятор) и ассемблер (код для эмулятора).

Вторая: C (прошивка) и конфигурационные файлы (PlatformIO, Renode).

Целевая платформа:

Первая: собственный 8-битный микроконтроллер с 16 регистрами и 64KB памяти.

Вторая: STM32F103C8T6 (ARM Cortex-M3), 32-битный микроконтроллер.

Функциональность:

Первая: реализация набора инструкций (16 инструкций) и выполнение программ на этом наборе.

Вторая: использование конкретной периферии (UART) для вывода сообщения, работа с регистрами микроконтроллера.

Инструменты:

Первая: только Python для запуска эмулятора.

Вторая: набор инструментов (VS Code, PlatformIO, Renode) для сборки и эмуляции.

Сложность:

Первая: более низкоуровневая, но в рамках собственного простого процессора.

Вторая: работа с реальным микроконтроллером, требующая знаний его периферии и инструментов链.

Назначение:

Первая: образовательная, чтобы понять, как работает микроконтроллер на уровне инструкций.

Вторая: практическая, чтобы научиться создавать прошивки для конкретного микроконтроллера и тестировать их в эмуляторе.

Теперь сделаем отсчет (сравнение) по пунктам:

Эмулятор vs Прошивка:

В первой работе вы создали эмулятор, который может выполнять программы для вашего собственного процессора.

Во второй работе вы создали прошивку, которая выполняется на существующем процессоре (STM32) в эмуляторе Renode.

Архитектура:

Первая: 8-битная, 16 регистров, 64KB памяти.

Вторая: 32-битная (ARM Cortex-M3), с гораздо большим объемом памяти и регистрами, специфичными для ARM.

Система команд:

Первая: собственная, состоящая из 16 инструкций.

Вторая: используется набор инструкций ARM Thumb, но в коде прошивки вы работаете на языке C, который компилируется в эти инструкции.

Периферия:

Первая: в эмуляторе не указана периферия, только процессор и память.

Вторая: используется UART для вывода сообщения, а также тактирование и настройка GPIO.

Инструменты и окружение:

Первая: требуется только Python.

Вторая: требуется установка VS Code, PlatformIO, Renode, а также знание работы с регистрами микроконтроллера.

Сложность кода:

Первая: код эмулятора на Python, который интерпретирует инструкции.

Вторая: код на C, который напрямую обращается к регистрам микроконтроллера для настройки периферии.

Вывод данных:

Первая: в эмуляторе вывод, скорее всего, осуществляется в консоль или логи эмулятора.

Вторая: вывод через UART, который отображается в анализаторе Renode.

Образовательная ценность:

Первая: помогает понять основы работы процессора, как выполняются инструкции, как работает память и регистры.

Вторая: помогает понять, как работать с реальными микроконтроллерами, настраивать периферию и использовать инструменты для разработки и отладки.

В итоге, эти две работы совершенно разного уровня и направленности. Первая - это создание виртуального процессора, вторая - написание программы для реального (эмулируемого) процессора с использованием профессиональных инструментов.

Отличный вопрос! Давайте сделаем детальный сравнительный анализ этих двух работ:

📊 СРАВНИТЕЛЬНАЯ ТАБЛИЦА
Критерий	Первая работа	Вторая работа
Тип проекта	Эмулятор процессора	Прошивка для реального МК
Уровень абстракции	Аппаратная эмуляция	Программное обеспечение
Архитектура	Собственная 8-битная	ARM Cortex-M3 (STM32)
Язык реализации	Python 94.6%, ASM 5.4%	C (прошивка), конфиги
Сложность	Высокая (эмуляция CPU)	Средняя (работа с периферией)
Практическое применение	Образовательная	Производственная
🎯 ОСНОВНЫЕ ОТЛИЧИЯ
1. СУТЬ ПРОЕКТА
Первая: Создание виртуального процессора "с нуля"

Вторая: Написание кода для существующего железа

2. АРХИТЕКТУРА
python
# Первая работа - собственная архитектура
8-битные регистры R0-R15
16 инструкций
64KB памяти
Собственная система команд

# Вторая работа - промышленная архитектура  
STM32F103C8T6 (Cortex-M3)
32-битная архитектура ARM
Реальные периферийные модули
3. СЛОЖНОСТЬ РЕАЛИЗАЦИИ
Первая:

Эмуляция процессорного ядра

Система команд

Работа с памятью

Обработка прерываний

Вторая:

Работа с регистрами МК

Настройка периферии (UART)

Интеграция с инструментами

4. ИНСТРУМЕНТАРИЙ
text
Первая работа:
├── Python
└── Простой запуск

Вторая работа:
├── VS Code + PlatformIO
├── STM32Cube Framework
├── Renode (эмулятор)
├── Компилятор ARM GCC
└── Отладочные инструменты
